# open/ndscm

Builds the open-source fork of `ndscm` from the `theseed` monorepo.

## What it does

Exports a curated subset of files from the main worktree at `theseed/main`,
strips unrelated modules, and rebuilds the history into a clean open-source
worktree.
