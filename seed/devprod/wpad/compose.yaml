services:
  wpad:
    container_name: "wpad"
    build: .
    environment:
      - WPAD_TARGET
    ports:
      - 9723:9723
    networks:
      - mbiz
    restart: unless-stopped

networks:
  mbiz:
    name: mbiz
    external: true
